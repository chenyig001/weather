name: 'GitHub Actions Weather Bot'
 
on:
  push:
  schedule:
    - cron: '0 24 * * *'

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
      - name: 'Get Weather'
        run: python weather.py
